<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <% if ((typeof(transData) != 'undefined') && transData.pageTitle) { %>
      <title><%= transData.pageTitle %></title>
    <% } else { %>
        <title>E-Shop</title>
    <% }; %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <% include stylesheets.ejs %>
  </head>

  <body>

    <!-- Navbar
    ================================================== -->
    <nav class="navbarTopk8 navbar-expand-lg navbar-dark bg-primary-topk8">
      <div class="container">

            <div class="navbar-topk8-tel">
                <div
                class="telefon-topk8"
                data-toggle="modal"
                data-target="#headerContactsModal">
                <span class="fas fa-phone avesomePaddingRight"></span>
                (067) 32-67-467
              </div>
            </div>
            <div class="navbar-topk8-usermenu">



                <% if ((typeof(transData) != 'undefined') && transData.user.login && transData.user._id) { %>
                  <span class="fas fa-user avesomePaddingRight"></span>
                  <a class="my-profile-link-k8" href="/profile/myprofile"><%= transData.user.login %></a>
                  <span class="fas fa-sign-out-alt avesomePaddingRight avesomePaddingLeft"></span>
                  <a class="my-profile-link-k8" href="/api/auth/logout">Выход</a>
                <% } else { %>
                  <span class="fas fa-sign-in-alt avesomePaddingRight"></span>
                  <a class="my-profile-link-k8" href="/login">Войдите в личный кабинет</a>
                <% } %>




            </div>
      </div>
    </nav>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">

        <a class="navbar-brand" href="/">Shvab E-Shop NodeJS</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarColor01">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item">
              <a class="nav-link" href="/#">Каталог товаров temp</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Приклад
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="/#">Типография</a>
                <a class="dropdown-item" href="/#">Каталог сатей</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#">Something else here</a>
              </div>
            </li>
           
          </ul>

            <button type="button" class="btn btn-success"><span class="fas fa-shopping-cart avesomePaddingRight"></span>Корзина<span class="badge badge-pill badge-shop-k8 badge-warning">1</span></button>
            
        
          
        </div>
      </div>
    </nav>



    <% include header_tel_modal.ejs %>
    