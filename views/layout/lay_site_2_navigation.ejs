<nav class="navbarTopk8 navbar-expand-lg navbar-dark bg-primary-topk8">
  <div class="container">
    <div class="navbar-topk8-tel">
      <div
        class="telefon-topk8"
        data-toggle="modal"
        data-target="#headerContactsModal"
      >
        <span class="fas fa-phone avesomePaddingRight"></span> (067) 32-67-467
      </div>
    </div>
    <div class="navbar-topk8-usermenu">
      <% if ((typeof(transData) != 'undefined') && transData.user.login &&
      transData.user._id) { %>
      <span class="fas fa-user avesomePaddingRight"></span>
      <a class="my-profile-link-k8" href="/profile/myprofile"
        ><%= transData.user.login %></a
      >
      <span
        class="fas fa-sign-out-alt avesomePaddingRight avesomePaddingLeft"
      ></span>
      <a class="my-profile-link-k8" href="/api/auth/logout">Выход</a> <% } else
      { %> <span class="fas fa-sign-in-alt avesomePaddingRight"></span>
      <a class="my-profile-link-k8" href="/login">Войдите в личный кабинет</a>
      <% } %>
    </div>
  </div>
</nav>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="/">Shvab E-Shop NodeJS</a>

    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarColor01"
      aria-controls="navbarColor01"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href="/shop">Каталог товаров</a>
        </li>
      </ul>

      <button type="button" class="btn btn-success">
        <span class="fas fa-shopping-cart avesomePaddingRight"></span>Корзина

        <span class="badge badge-pill badge-shop-k8 badge-warning">
          <div id="topshopbuttonitemsquantity">
            <% if ((typeof(transData) != 'undefined') && transData.shopCart) {
            %> <%= transData.shopCart.length %> <% } else { %> 0 <% } %>
          </div>
        </span>
      </button>
    </div>
  </div>
</nav>

<!-- Modal -->
<div
  class="modal fade"
  id="headerContactsModal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="headerContactsModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="headerContactsModalLabel">Контакты</h5>
        <button
          type="button"
          class="close"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">...Текст гоус хіре</div>
      <div class="modal-footer">somthing in footer</div>
    </div>
  </div>
</div>
